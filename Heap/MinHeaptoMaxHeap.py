# The task is to convert a min heap to max heap
# The tricky part is that they are confusing us giving
# that we have min heap and convert it to max heap. but
# given an unsorted array, we can build a max heap in 
# O(n) time

from heapq import heapify

def mintomax(arrayy):
    return heapify(array)
